# CRM V1.0.0-rc.4 候选版本记录

发布日期：2026-06-18

计划 Git tag：

```text
v1.0.0-rc.4
```

## 1. 候选版本定位

`v1.0.0-rc.4` 是在 `v1.0.0-rc.3` 基础上修正本地具名验证环境证据版本漂移，并加严 RC/UAT readiness 审计的 V1 候选版本，用于继续推进具名测试环境 UAT 和 Go/No-Go 准出。

该版本包含：

- `v1.0.0-rc.3` 已具备的本地具名验证环境证据。
- `scripts/v1-uat-readiness-check.mjs` 新增本地 UAT evidence 与当前 RC 的一致性检查，避免证据包仍指向旧候选版本。
- `scripts/v1-uat-readiness-check.test.mjs` 新增“本地 UAT evidence 指向旧 RC 时失败”的回归测试。
- `docs/testing/evidence/v1-local-uat-2026-06-18.md` 候选版本元数据修正为 `v1.0.0-rc.4`。

该版本已完成研发侧自动化验证、部署配置校验、RC/UAT 就绪审计和本地服务级具名环境验证；仍需在项目指定具名测试环境完成 UI 级浏览器 Smoke、UAT 演示、缺陷闭环和销售侧/管理侧验收签署后，才能升级为正式 V1 发布版本。

## 2. V1范围

本候选版本覆盖：

- 登录、登出、修改密码、管理员重置密码。
- 组织、用户、角色、权限、数据范围和字典维护。
- 客户池、联系人与干系人、商机生命周期、销售行动、提醒待办、附件元数据。
- 销售行动完成后回写客户和商机最近跟进。
- 商机周进展按已完成销售行动自动汇总。
- 关键操作审计日志。
- V1演示管理员种子和部署态 bootstrap 探针。
- 前端 V1 基础页面、核心对象筛选、详情入口和主要编辑操作。

本候选版本不包含：

- 方案标书。
- 合同管理。
- 开票和回款管理。
- 经营驾驶舱完整指标。
- AI销售助手完整能力。

## 3. 自动化验证

本地验证：

| 命令 | 结果 |
|---|---|
| `node --test scripts/v1-uat-readiness-check.test.mjs` | 3 tests passed |
| `node scripts/v1-uat-readiness-check.mjs` | RC/UAT readiness check passed |
| `node --test ../scripts/v1-uat-readiness-check.test.mjs` | 3 tests passed；覆盖CI前端job相对路径 |
| `git diff --check` | 通过 |

继承 `v1.0.0-rc.3` 已通过的验证：

| 命令 | 结果 |
|---|---|
| `ruby -e "require 'yaml'; ..."` | GitHub Actions YAML 结构通过 |
| `docker compose -f compose.v1-test.yml config` | Compose 配置展开通过 |
| `mvn test` | 56 tests passed |
| `mvn verify -Ppostgres-it` | 56 surefire tests passed；1 PostgreSQL integration test passed |
| `npm test` | 16 tests passed |
| `npm run build` | Build succeeded；保留 antd vendor chunk 体积提示 |

远端验证：

| 工作流 | Run | 结论 |
|---|---|---|
| GitHub Actions `V1 Validation` | 以 `v1.0.0-rc.4` 所属提交的 GitHub Actions run 为权威记录 | 准出要求为 success |

远端 job 必须覆盖：

- `Deployment config validation`：包含 Compose 配置校验和 RC/UAT readiness 审计。
- `Backend tests and PostgreSQL migration`。
- `Frontend tests and production build`：包含 readiness checker tests。

## 4. 本地具名验证环境

环境名称：

```text
V1-local-uat-20260618
```

证据记录：

```text
docs/testing/evidence/v1-local-uat-2026-06-18.md
```

已验证：

- PostgreSQL `postgres:16` 容器 healthy。
- Flyway 在空库执行 14 个迁移脚本。
- 后端 `/api/health` 返回 `OK`。
- `demo_admin` 登录后 `/api/bootstrap` 返回 200，权限数 25，V1演示组织/角色/用户种子可见。
- 前端 `http://127.0.0.1:5175/system` 返回 HTTP 200。

## 5. 部署入口

测试环境 Compose：

```bash
docker compose -f compose.v1-test.yml up -d --build
```

部署说明：

```text
docs/deployment/v1-test-environment-compose.md
```

默认演示管理员：

```text
username: demo_admin
password: S3cure!123
```

正式试点或生产环境不得启用演示种子，应将 `CRM_SEED_V1_DEMO_ENABLED` 设置为 `false`。

## 6. 已知限制

- 当前机器访问 Docker Hub token 接口曾超时，因此本地未完成 `docker compose -f compose.v1-test.yml up -d --build` 的完整后端/前端镜像启动；Compose 配置、后端 package、PostgreSQL迁移集成、前端 build 和本地服务级验证已分别通过。
- 当前 Browser 插件访问本地 URL 触发 Browser Use URL policy 阻断，因此 UI 级浏览器 Smoke 仍需在可访问目标环境的测试执行机上执行并归档截图。
- 业务验收人、具名测试环境域名、正式测试账号和业务样例数据仍待项目/测试/业务侧确认。
- V1正式发布前必须完成 UAT 证据包和 Go/No-Go 记录。

## 7. UAT执行入口

- 具名测试环境执行 Runbook：`docs/testing/crm-v1-test-environment-validation-runbook.md`
- UAT 证据包模板：`docs/testing/crm-v1-uat-evidence-pack-template.md`
- 验收清单：`docs/testing/crm-v1-acceptance-checklist.md`
- RC/UAT就绪审计：`node scripts/v1-uat-readiness-check.mjs`
