# 审批与 AI 助手功能测试报告

日期：2026-07-14
环境：`http://120.55.73.23/`
测试数据前缀：`FT-AIAPP-0714`

## 结论

- 通过 27 项、失败 3 项、阻塞或未测 16 项。
- 缺陷：P0 0、P1 1、P2 1、P3 0。
- 审批模块因公网仍为 V35 版本而整体阻塞；AI 人工确认写入主链路通过。

## 已通过

- 草稿 `3` 确认后写入客户 `9`。
- 草稿 `4` 确认后写入联系人 `4`。
- 草稿 `5` 确认后写入商机 `10`。
- 草稿 `6` 确认后写入行动 `3`。
- 草稿 `7` 无法识别时返回 `need_more_info`，人工拒绝成功且未写入。
- 草稿 `8` 确认后写入隔离行动 `4`。
- 周报生成、拒绝以及待确认状态通过；共享 V4 API smoke 验证五类 AI 模块与日志可访问。

## 缺陷

### APPROVAL-001 P1 审批模块在公网版本不可用

- 审批模板、审批角色和审批待办均返回 HTTP 500。
- 后端日志显示路由不存在；数据库 Flyway 最新版本为 V35，当前源码审批迁移为 V36-V38。
- Trace：`FT-AIAPP-0714-probe-01`、`FT-AIAPP-0714-probe-02`、`FT-AIAPP-0714-probe-03`。

### AI-001 P2 未配置模型时缺少明确降级说明

- 前置：AI 模型配置为 0 条。
- 实际：文本草稿和周报仍生成成功，但响应和页面未说明使用远程模型、规则引擎或降级模式。
- 影响：用户无法判断结果来源、能力边界和配置是否生效。
- Trace：`FT-AIAPP-0714-draft-account-parse`、`FT-AIAPP-0714-weekly18-generate`。

## 阻塞或未测

- 审批模板创建、编辑、启停和节点配置。
- 报价、投标、合同发起审批、同意、驳回、撤回和重复操作限制。
- 审批人、发起人的权限边界及审批中心状态同步。
- 周报人工确认写入、商机分析、拜访计划和沟通建议的全部确认/拒绝组合。
- 本轮新数据在 AI 日志中的 Trace/Object ID 完整性和公网 UI 提示。

说明：当前前端/API 契约中未发现审批撤回接口，需要单独确认产品范围。
